# 배너 이미지 넣는 곳

1. 이 폴더에 배너 이미지를 넣습니다. 권장 크기는 `1600 × 500px`입니다.
2. 파일 이름은 `banner.jpg`처럼 영문으로 정하는 것을 권장합니다.
3. `../js/config.js` 파일의 `imageUrl`을 아래처럼 수정합니다.

```js
imageUrl: "./assets/banner/banner.jpg",
```

모바일에서는 같은 이미지가 세로로 잘려 보일 수 있으므로, 중요한 내용은 이미지 중앙에 배치하세요.
